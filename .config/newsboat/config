# Newsboat configuration file
# See `man newsboat` for documentation
# Yoyomanzoor

reload-time 3600
browser "vieb %u"
include /usr/share/doc/newsboat/contrib/colorschemes/inkpot
goto-next-feed no
show-read-feeds yes

# bind keys - vim style
unbind-key j
unbind-key k
unbind-key l
unbind-key h
bind j everywhere down
bind k everywhere up
bind ^b everywhere pageup
bind ^f everywhere pagedown
bind ^d everywhere halfpagedown
bind ^u everywhere halfpageup

unbind-key g
unbind-key G
bind gg everywhere home
bind G everywhere end
bind gs everywhere sort
bind gr everywhere rev-sort
bind ^i everywhere prev-dialog
bind ^o everywhere next-dialog

bind J articlelist next-feed
bind K articlelist prev-feed
bind l everywhere open

bind h feedlist view-dialogs
bind h filebrowser quit
bind h help quit
bind h articlelist quit
bind h article quit
bind h tagselection quit
bind h filterselection quit
bind h urlview quit
bind h podboat quit
bind h dirbrowser quit
bind h searchresultslist quit

bind c everywhere clear-filter
bind a everywhere article-feed
bind gh everywhere view-dialogs

bind ^p everywhere macro-prefix
macro o set browser "boatreader %u"; open-in-browser; set browser "vieb %u"
macro w set browser "w3m %u"; open-in-browser; set browser "vieb %u"
macro l set browser "lynx %u"; open-in-browser; set browser "vieb %u"
macro f set browser "firefox %u"; open-in-browser; set browser "vieb %u"
macro v set browser "vieb %u"; open-in-browser
macro y set browser "wl-copy %u"; open-in-browser; set browser "vieb %u"

bookmark-cmd bookmarks
bookmark-interactive no
bookmark-autopilot yes
save-path ~/.local/share/rss/

# errors
error-log ~/.config/newsboat/error.log

# heading
highlight feedlist "----.*----" yellow default bold
highlight feedlist ".*0/0.." default default invis

# filters
define-filter hide-preprint "(tags !# \"source-preprint\")"
define-filter show-opensource "(tags # \"access-open-access\")"
define-filter sections "(tags # \"section\")"
